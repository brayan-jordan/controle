package com.example.controle.domain.repository;

import com.example.controle.domain.model.DiaDeAula;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DiaDeAulaRepository extends JpaRepository<DiaDeAula, Long> {
}
